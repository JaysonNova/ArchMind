# 国际化 (i18n) 快速使用指南

## 🌍 功能概述

ArchMind AI 现已支持**中英文双语切换**，用户可以在应用的任何页面一键切换语言。

## 🚀 快速开始

### 切换语言

在页面顶部找到语言切换器（显示为当前语言），点击即可选择：
- 🇨🇳 **简体中文**
- 🇺🇸 **English**

语言设置会自动保存，下次访问时自动恢复您的选择。

## 📝 开发者指南

### 在代码中添加翻译

#### 1. 模板中使用 `$t()`

```vue
<template>
  <!-- 基本用法 -->
  <h1>{{ $t('page.title') }}</h1>

  <!-- 带参数 -->
  <p>{{ $t('message.greeting', { name: 'User' }) }}</p>

  <!-- 在属性中使用 -->
  <Input :placeholder="$t('form.placeholder')" />
</template>
```

#### 2. Script 中使用 `t()`

```vue
<script setup lang="ts">
const { t } = useI18n()

// 在函数中
function handleSave() {
  alert(t('common.saved'))
}

// 在计算属性中
const title = computed(() => t('page.title'))
</script>
```

### 添加新翻译

1. 编辑 `i18n/lang/zh-CN.json`:
```json
{
  "myFeature": {
    "title": "我的功能",
    "description": "功能描述"
  }
}
```

2. 编辑 `i18n/lang/en.json`:
```json
{
  "myFeature": {
    "title": "My Feature",
    "description": "Feature description"
  }
}
```

3. 在组件中使用:
```vue
<h2>{{ $t('myFeature.title') }}</h2>
```

## 🎯 翻译键结构

```
app.*              - 应用级别（应用名称等）
nav.*              - 导航项
common.*           - 通用文本（按钮、操作等）
projects.*         - 项目页面
generate.*         - 生成页面
documents.*        - 文档页面
settings.*         - 设置页面
chat.*             - 聊天组件
```

## 💡 常用示例

### 确认对话框

```typescript
if (confirm(t('projects.deleteConfirm'))) {
  // 执行删除
}
```

### 表单占位符

```vue
<Input :placeholder="$t('chat.inputPlaceholder')" />
```

### 带参数的消息

```vue
{{ $t('documents.upload.uploading', { progress: 50 }) }}
// 输出: "上传中... 50%"
```

## 📚 完整文档

详细文档请查看: [docs/I18N.md](./I18N.md)

## 🐛 问题反馈

如果发现翻译问题或缺失，请：
1. 检查 `i18n/lang/` 目录下的翻译文件
2. 确认翻译键是否存在
3. 提交 Issue 或 Pull Request
